import React, { useState } from "react";

const MapPhotoItem = (props: {
  // id: number;
  // url: any;
}) => {
  // const { id, url } = props;


  return (
    <div></div>
  );
};

export default MapPhotoItem;
